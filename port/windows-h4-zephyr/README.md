# BTstack Port for Windows Systems with Zephyr-based Controller

This port has been moved to `port/archive/windows-h4-zephyr`.

Zephyr-based Controllers are supported by the regular `windows-h4` port. Use baudrate `1000000`, e.g.:

    ./gatt_counter.exe -u COM3 --baud 1000000
